---
buildpack: ruby_buildpack
services:
  - database
  - redis
applications:
  - name: william
    memory: 512M
    host: vm-tester
    command: bundle exec rake db:migrate && bundle exec puma -p $PORT
  - name: william-worker
    memory: 512M
    no-route: true


